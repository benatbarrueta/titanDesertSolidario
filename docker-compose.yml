version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: titan_desert_backend
    restart: unless-stopped
    volumes:
      - ./titanDB/data:/data
    ports:
      - "8000:8000"
    environment:
      - SQLITE_DB_PATH=/data/titan_desert_solidario.db
    networks:
      - titan_network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: titan_desert_frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - backend
    environment:
      - REACT_APP_API_BASE_URL=/api/v1
    networks:
      - titan_network

networks:
  titan_network:
    external: true